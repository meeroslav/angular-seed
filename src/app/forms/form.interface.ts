export interface User {
  name: string;
  favoriteNumber: number;
  favoriteColor: string;
  observation: string;
  optin: boolean;
  newsLetter: boolean;
}

export interface Colors {
  id: number;
  color: string
}
